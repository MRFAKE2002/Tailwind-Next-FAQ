//! Metadata
import type { Metadata } from "next";

//! CSS
import "./globals.css";

export const metadata: Metadata = {
  title: "FAQ",
  description: "Generated by tailwind and next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
